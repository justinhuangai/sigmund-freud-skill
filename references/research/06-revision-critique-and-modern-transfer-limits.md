# 修正、批评与现代转译边界 / Revision, Critique, and Modern Transfer Limits

What survives, what has been revised, and how to transfer Freud without pseudo-clinical overreach.

## Why This File Exists

- This file supports the Sigmund Freud person skill by making the research layer thick enough to guide real reasoning rather than surface style.
- It treats 弗洛伊德 as a structured cognitive system, not as a quotation source or prestige label.
- The focus of this file is What survives, what has been revised, and how to transfer Freud without pseudo-clinical overreach..
- The notes below are organized to keep historical grounding, operational transfer, and misuse boundaries visible at the same time.

## Source Orientation

- Read the primary texts or documentary anchors first, then map them to later interpretation.
- Keep biography, corpus boundary, and later myth separate from direct conceptual claims.
- When transferring this material into a modern skill, preserve the structure and drop the historical costume.

## Research Notes

### empirical criticism — Definition

- What this names: within `What survives, what has been revised, and how to transfer Freud without pseudo-clinical overreach`, `empirical criticism` points to a concrete node rather than a prestige label.
- Object boundary: treat `empirical criticism` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `empirical criticism` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `empirical criticism` before pulling it into a modern analogy.
- Skill value: `empirical criticism` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `empirical criticism` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `empirical criticism` clearly enough that a later answer can say what `empirical criticism` includes and excludes.

### empirical criticism — Historical Context

- Historical location: ask when `empirical criticism` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `empirical criticism` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `empirical criticism` says, but why `empirical criticism` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `empirical criticism` from later packaging or selective reuse.
- Reading move: map `empirical criticism` against the timeline before treating `empirical criticism` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `empirical criticism` before extracting any lesson from `empirical criticism`.
- Failure risk: without context, `empirical criticism` can look cleaner, more universal, and more innocent than it actually was.

### empirical criticism — Internal Tension

- Main tension: `empirical criticism` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `empirical criticism` makes possible and what `empirical criticism` quietly suppresses at the same time.
- Interpretive value: the tension in `empirical criticism` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `empirical criticism` double-sided rather than converting `empirical criticism` into moral reassurance.
- Research warning: if every sentence about `empirical criticism` sounds admiring or dismissive, `empirical criticism` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `empirical criticism`.
- Better question: what does `empirical criticism` solve, and what new silence does `empirical criticism` create?

### empirical criticism — Key Distinction

- Core distinction: separate `empirical criticism` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `empirical criticism`.
- Typical confusion: readers often mistake `empirical criticism` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `empirical criticism` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `empirical criticism`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `empirical criticism` by accident.
- Operational cue: define what `empirical criticism` is not before claiming to know what `empirical criticism` is.

### empirical criticism — Method

- Method question: how does `empirical criticism` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `empirical criticism` rather than inspirational phrasing.
- Research use: `empirical criticism` matters when `empirical criticism` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `empirical criticism`, not costume around `empirical criticism`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `empirical criticism`.
- Modern move: only transfer the mechanism in `empirical criticism` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `empirical criticism` without reproducing the method inside `empirical criticism`.

### empirical criticism — Operational Use

- Operational role: `empirical criticism` belongs in the skill because `empirical criticism` changes how prompts are framed and how problems are cut.
- Decision value: `empirical criticism` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `empirical criticism` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `empirical criticism` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `empirical criticism` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `empirical criticism` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `empirical criticism` make me check before I act?

### empirical criticism — Transfer To Modern Problems

- Transfer principle: bring `empirical criticism` forward only when the structural analogy around `empirical criticism` is stronger than the historical costume.
- Modern fit: `empirical criticism` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `empirical criticism` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `empirical criticism`, not the surrounding mythology around `empirical criticism`.
- Bad transfer: using `empirical criticism` to justify positions `empirical criticism` was never built to adjudicate.
- Practical test: if the analogy for `empirical criticism` cannot survive plainer language, `empirical criticism` probably does not belong in the answer.
- Design implication: every modern use of `empirical criticism` should make the analogy explicit instead of pretending `empirical criticism` is self-evident.

### empirical criticism — Failure Mode

- Failure pattern: `empirical criticism` goes bad when `empirical criticism` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `empirical criticism` for authority, mood, or aesthetic and lose the underlying discipline in `empirical criticism`.
- Research signal: when `empirical criticism` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `empirical criticism`.
- User risk: answers can start sounding impressive while `empirical criticism` becomes less falsifiable.
- Repository risk: `empirical criticism` can silently drift from operating concept into mascot.
- Correction move: pull `empirical criticism` back to evidence, mechanism, and concrete limits.
- Best warning label: `empirical criticism` should increase rigor, not lower the burden of proof.

### empirical criticism — Common Misreading

- Typical misreading: `empirical criticism` is often taken as a broad worldview when `empirical criticism` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `empirical criticism`.
- Consequence: readers then import claims into `empirical criticism` that the corpus does not actually support for `empirical criticism`.
- Skill response: keep `empirical criticism` attached to the texts, cases, and institutional situations that make `empirical criticism` legible.
- Modern correction: show what `empirical criticism` can illuminate without pretending `empirical criticism` answers everything nearby.
- Language fix: name the overreach around `empirical criticism` directly instead of just gesturing at nuance.
- Reader test: if `empirical criticism` suddenly explains every problem, `empirical criticism` is being misread.

### empirical criticism — Boundary Condition

- Boundary condition: `empirical criticism` only travels well when the receiving problem shares enough structure with the original one behind `empirical criticism`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `empirical criticism`.
- Ethical test: some uses of `empirical criticism` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `empirical criticism` into careless advice.
- Good limit: it is fine for `empirical criticism` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `empirical criticism` has no boundary because `empirical criticism` feels generically wise.
- Operational question: under what conditions should `empirical criticism` be left unused?

### empirical criticism — Better Question

- Better question: instead of asking whether `empirical criticism` is simply true, ask what problem `empirical criticism` was trying to solve.
- Reader upgrade: this turns `empirical criticism` from a label into a live diagnostic instrument.
- Research upgrade: `empirical criticism` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `empirical criticism` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `empirical criticism` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `empirical criticism` becomes useful when `empirical criticism` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `empirical criticism` is framed as a problem-solving move?

### empirical criticism — Example Move

- Example move: use `empirical criticism` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `empirical criticism` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `empirical criticism` is strongest when `empirical criticism` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `empirical criticism` lets the user do that they could not do before.
- Transfer discipline: keep the move around `empirical criticism` small and explicit so the analogy does not sprawl.
- Repository standard: if `empirical criticism` never produces a concrete move, `empirical criticism` is not yet operational.
- User question: what is one thing `empirical criticism` would make me inspect differently right now?

### empirical criticism — What To Preserve

- Preserve the pressure: `empirical criticism` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `empirical criticism` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `empirical criticism`.
- Preserve the routing value: `empirical criticism` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `empirical criticism` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `empirical criticism` stops is part of preserving `empirical criticism` well.
- Preserve the usefulness: a transferred `empirical criticism` should still help someone make a cleaner judgment.

### empirical criticism — What To Drop

- Drop the costume: `empirical criticism` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `empirical criticism` has been understood.
- Drop the universal claim: `empirical criticism` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `empirical criticism` survives only as a catchphrase, `empirical criticism` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `empirical criticism` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `empirical criticism` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `empirical criticism` and remove the surrounding noise.

### empirical criticism — Design Implication

- Design implication: `empirical criticism` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `empirical criticism` that later edits can stay honest.
- UX implication: users should feel `empirical criticism` changing the angle of analysis, not just the tone.
- Evaluation implication: test `empirical criticism` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `empirical criticism` only if they sharpen the boundary of `empirical criticism` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `empirical criticism` should refuse.
- Bottom line: `empirical criticism` belongs in the repo only if `empirical criticism` keeps improving judgment after the historical costume is removed.

### sex and reductionism — Definition

- What this names: within `What survives, what has been revised, and how to transfer Freud without pseudo-clinical overreach`, `sex and reductionism` points to a concrete node rather than a prestige label.
- Object boundary: treat `sex and reductionism` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `sex and reductionism` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `sex and reductionism` before pulling it into a modern analogy.
- Skill value: `sex and reductionism` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `sex and reductionism` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `sex and reductionism` clearly enough that a later answer can say what `sex and reductionism` includes and excludes.

### sex and reductionism — Historical Context

- Historical location: ask when `sex and reductionism` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `sex and reductionism` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `sex and reductionism` says, but why `sex and reductionism` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `sex and reductionism` from later packaging or selective reuse.
- Reading move: map `sex and reductionism` against the timeline before treating `sex and reductionism` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `sex and reductionism` before extracting any lesson from `sex and reductionism`.
- Failure risk: without context, `sex and reductionism` can look cleaner, more universal, and more innocent than it actually was.

### sex and reductionism — Internal Tension

- Main tension: `sex and reductionism` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `sex and reductionism` makes possible and what `sex and reductionism` quietly suppresses at the same time.
- Interpretive value: the tension in `sex and reductionism` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `sex and reductionism` double-sided rather than converting `sex and reductionism` into moral reassurance.
- Research warning: if every sentence about `sex and reductionism` sounds admiring or dismissive, `sex and reductionism` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `sex and reductionism`.
- Better question: what does `sex and reductionism` solve, and what new silence does `sex and reductionism` create?

### sex and reductionism — Key Distinction

- Core distinction: separate `sex and reductionism` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `sex and reductionism`.
- Typical confusion: readers often mistake `sex and reductionism` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `sex and reductionism` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `sex and reductionism`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `sex and reductionism` by accident.
- Operational cue: define what `sex and reductionism` is not before claiming to know what `sex and reductionism` is.

### sex and reductionism — Method

- Method question: how does `sex and reductionism` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `sex and reductionism` rather than inspirational phrasing.
- Research use: `sex and reductionism` matters when `sex and reductionism` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `sex and reductionism`, not costume around `sex and reductionism`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `sex and reductionism`.
- Modern move: only transfer the mechanism in `sex and reductionism` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `sex and reductionism` without reproducing the method inside `sex and reductionism`.

### sex and reductionism — Operational Use

- Operational role: `sex and reductionism` belongs in the skill because `sex and reductionism` changes how prompts are framed and how problems are cut.
- Decision value: `sex and reductionism` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `sex and reductionism` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `sex and reductionism` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `sex and reductionism` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `sex and reductionism` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `sex and reductionism` make me check before I act?

### sex and reductionism — Transfer To Modern Problems

- Transfer principle: bring `sex and reductionism` forward only when the structural analogy around `sex and reductionism` is stronger than the historical costume.
- Modern fit: `sex and reductionism` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `sex and reductionism` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `sex and reductionism`, not the surrounding mythology around `sex and reductionism`.
- Bad transfer: using `sex and reductionism` to justify positions `sex and reductionism` was never built to adjudicate.
- Practical test: if the analogy for `sex and reductionism` cannot survive plainer language, `sex and reductionism` probably does not belong in the answer.
- Design implication: every modern use of `sex and reductionism` should make the analogy explicit instead of pretending `sex and reductionism` is self-evident.

### sex and reductionism — Failure Mode

- Failure pattern: `sex and reductionism` goes bad when `sex and reductionism` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `sex and reductionism` for authority, mood, or aesthetic and lose the underlying discipline in `sex and reductionism`.
- Research signal: when `sex and reductionism` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `sex and reductionism`.
- User risk: answers can start sounding impressive while `sex and reductionism` becomes less falsifiable.
- Repository risk: `sex and reductionism` can silently drift from operating concept into mascot.
- Correction move: pull `sex and reductionism` back to evidence, mechanism, and concrete limits.
- Best warning label: `sex and reductionism` should increase rigor, not lower the burden of proof.

### sex and reductionism — Common Misreading

- Typical misreading: `sex and reductionism` is often taken as a broad worldview when `sex and reductionism` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `sex and reductionism`.
- Consequence: readers then import claims into `sex and reductionism` that the corpus does not actually support for `sex and reductionism`.
- Skill response: keep `sex and reductionism` attached to the texts, cases, and institutional situations that make `sex and reductionism` legible.
- Modern correction: show what `sex and reductionism` can illuminate without pretending `sex and reductionism` answers everything nearby.
- Language fix: name the overreach around `sex and reductionism` directly instead of just gesturing at nuance.
- Reader test: if `sex and reductionism` suddenly explains every problem, `sex and reductionism` is being misread.

### sex and reductionism — Boundary Condition

- Boundary condition: `sex and reductionism` only travels well when the receiving problem shares enough structure with the original one behind `sex and reductionism`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `sex and reductionism`.
- Ethical test: some uses of `sex and reductionism` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `sex and reductionism` into careless advice.
- Good limit: it is fine for `sex and reductionism` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `sex and reductionism` has no boundary because `sex and reductionism` feels generically wise.
- Operational question: under what conditions should `sex and reductionism` be left unused?

### sex and reductionism — Better Question

- Better question: instead of asking whether `sex and reductionism` is simply true, ask what problem `sex and reductionism` was trying to solve.
- Reader upgrade: this turns `sex and reductionism` from a label into a live diagnostic instrument.
- Research upgrade: `sex and reductionism` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `sex and reductionism` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `sex and reductionism` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `sex and reductionism` becomes useful when `sex and reductionism` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `sex and reductionism` is framed as a problem-solving move?

### sex and reductionism — Example Move

- Example move: use `sex and reductionism` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `sex and reductionism` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `sex and reductionism` is strongest when `sex and reductionism` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `sex and reductionism` lets the user do that they could not do before.
- Transfer discipline: keep the move around `sex and reductionism` small and explicit so the analogy does not sprawl.
- Repository standard: if `sex and reductionism` never produces a concrete move, `sex and reductionism` is not yet operational.
- User question: what is one thing `sex and reductionism` would make me inspect differently right now?

### sex and reductionism — What To Preserve

- Preserve the pressure: `sex and reductionism` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `sex and reductionism` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `sex and reductionism`.
- Preserve the routing value: `sex and reductionism` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `sex and reductionism` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `sex and reductionism` stops is part of preserving `sex and reductionism` well.
- Preserve the usefulness: a transferred `sex and reductionism` should still help someone make a cleaner judgment.

### sex and reductionism — What To Drop

- Drop the costume: `sex and reductionism` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `sex and reductionism` has been understood.
- Drop the universal claim: `sex and reductionism` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `sex and reductionism` survives only as a catchphrase, `sex and reductionism` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `sex and reductionism` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `sex and reductionism` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `sex and reductionism` and remove the surrounding noise.

### sex and reductionism — Design Implication

- Design implication: `sex and reductionism` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `sex and reductionism` that later edits can stay honest.
- UX implication: users should feel `sex and reductionism` changing the angle of analysis, not just the tone.
- Evaluation implication: test `sex and reductionism` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `sex and reductionism` only if they sharpen the boundary of `sex and reductionism` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `sex and reductionism` should refuse.
- Bottom line: `sex and reductionism` belongs in the repo only if `sex and reductionism` keeps improving judgment after the historical costume is removed.

### enduring concepts — Definition

- What this names: within `What survives, what has been revised, and how to transfer Freud without pseudo-clinical overreach`, `enduring concepts` points to a concrete node rather than a prestige label.
- Object boundary: treat `enduring concepts` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `enduring concepts` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `enduring concepts` before pulling it into a modern analogy.
- Skill value: `enduring concepts` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `enduring concepts` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `enduring concepts` clearly enough that a later answer can say what `enduring concepts` includes and excludes.

### enduring concepts — Historical Context

- Historical location: ask when `enduring concepts` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `enduring concepts` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `enduring concepts` says, but why `enduring concepts` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `enduring concepts` from later packaging or selective reuse.
- Reading move: map `enduring concepts` against the timeline before treating `enduring concepts` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `enduring concepts` before extracting any lesson from `enduring concepts`.
- Failure risk: without context, `enduring concepts` can look cleaner, more universal, and more innocent than it actually was.

### enduring concepts — Internal Tension

- Main tension: `enduring concepts` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `enduring concepts` makes possible and what `enduring concepts` quietly suppresses at the same time.
- Interpretive value: the tension in `enduring concepts` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `enduring concepts` double-sided rather than converting `enduring concepts` into moral reassurance.
- Research warning: if every sentence about `enduring concepts` sounds admiring or dismissive, `enduring concepts` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `enduring concepts`.
- Better question: what does `enduring concepts` solve, and what new silence does `enduring concepts` create?

### enduring concepts — Key Distinction

- Core distinction: separate `enduring concepts` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `enduring concepts`.
- Typical confusion: readers often mistake `enduring concepts` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `enduring concepts` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `enduring concepts`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `enduring concepts` by accident.
- Operational cue: define what `enduring concepts` is not before claiming to know what `enduring concepts` is.

### enduring concepts — Method

- Method question: how does `enduring concepts` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `enduring concepts` rather than inspirational phrasing.
- Research use: `enduring concepts` matters when `enduring concepts` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `enduring concepts`, not costume around `enduring concepts`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `enduring concepts`.
- Modern move: only transfer the mechanism in `enduring concepts` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `enduring concepts` without reproducing the method inside `enduring concepts`.

### enduring concepts — Operational Use

- Operational role: `enduring concepts` belongs in the skill because `enduring concepts` changes how prompts are framed and how problems are cut.
- Decision value: `enduring concepts` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `enduring concepts` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `enduring concepts` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `enduring concepts` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `enduring concepts` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `enduring concepts` make me check before I act?

### enduring concepts — Transfer To Modern Problems

- Transfer principle: bring `enduring concepts` forward only when the structural analogy around `enduring concepts` is stronger than the historical costume.
- Modern fit: `enduring concepts` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `enduring concepts` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `enduring concepts`, not the surrounding mythology around `enduring concepts`.
- Bad transfer: using `enduring concepts` to justify positions `enduring concepts` was never built to adjudicate.
- Practical test: if the analogy for `enduring concepts` cannot survive plainer language, `enduring concepts` probably does not belong in the answer.
- Design implication: every modern use of `enduring concepts` should make the analogy explicit instead of pretending `enduring concepts` is self-evident.

### enduring concepts — Failure Mode

- Failure pattern: `enduring concepts` goes bad when `enduring concepts` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `enduring concepts` for authority, mood, or aesthetic and lose the underlying discipline in `enduring concepts`.
- Research signal: when `enduring concepts` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `enduring concepts`.
- User risk: answers can start sounding impressive while `enduring concepts` becomes less falsifiable.
- Repository risk: `enduring concepts` can silently drift from operating concept into mascot.
- Correction move: pull `enduring concepts` back to evidence, mechanism, and concrete limits.
- Best warning label: `enduring concepts` should increase rigor, not lower the burden of proof.

### enduring concepts — Common Misreading

- Typical misreading: `enduring concepts` is often taken as a broad worldview when `enduring concepts` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `enduring concepts`.
- Consequence: readers then import claims into `enduring concepts` that the corpus does not actually support for `enduring concepts`.
- Skill response: keep `enduring concepts` attached to the texts, cases, and institutional situations that make `enduring concepts` legible.
- Modern correction: show what `enduring concepts` can illuminate without pretending `enduring concepts` answers everything nearby.
- Language fix: name the overreach around `enduring concepts` directly instead of just gesturing at nuance.
- Reader test: if `enduring concepts` suddenly explains every problem, `enduring concepts` is being misread.

### enduring concepts — Boundary Condition

- Boundary condition: `enduring concepts` only travels well when the receiving problem shares enough structure with the original one behind `enduring concepts`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `enduring concepts`.
- Ethical test: some uses of `enduring concepts` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `enduring concepts` into careless advice.
- Good limit: it is fine for `enduring concepts` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `enduring concepts` has no boundary because `enduring concepts` feels generically wise.
- Operational question: under what conditions should `enduring concepts` be left unused?

### enduring concepts — Better Question

- Better question: instead of asking whether `enduring concepts` is simply true, ask what problem `enduring concepts` was trying to solve.
- Reader upgrade: this turns `enduring concepts` from a label into a live diagnostic instrument.
- Research upgrade: `enduring concepts` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `enduring concepts` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `enduring concepts` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `enduring concepts` becomes useful when `enduring concepts` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `enduring concepts` is framed as a problem-solving move?

### enduring concepts — Example Move

- Example move: use `enduring concepts` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `enduring concepts` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `enduring concepts` is strongest when `enduring concepts` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `enduring concepts` lets the user do that they could not do before.
- Transfer discipline: keep the move around `enduring concepts` small and explicit so the analogy does not sprawl.
- Repository standard: if `enduring concepts` never produces a concrete move, `enduring concepts` is not yet operational.
- User question: what is one thing `enduring concepts` would make me inspect differently right now?

### enduring concepts — What To Preserve

- Preserve the pressure: `enduring concepts` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `enduring concepts` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `enduring concepts`.
- Preserve the routing value: `enduring concepts` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `enduring concepts` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `enduring concepts` stops is part of preserving `enduring concepts` well.
- Preserve the usefulness: a transferred `enduring concepts` should still help someone make a cleaner judgment.

### enduring concepts — What To Drop

- Drop the costume: `enduring concepts` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `enduring concepts` has been understood.
- Drop the universal claim: `enduring concepts` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `enduring concepts` survives only as a catchphrase, `enduring concepts` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `enduring concepts` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `enduring concepts` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `enduring concepts` and remove the surrounding noise.

### enduring concepts — Design Implication

- Design implication: `enduring concepts` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `enduring concepts` that later edits can stay honest.
- UX implication: users should feel `enduring concepts` changing the angle of analysis, not just the tone.
- Evaluation implication: test `enduring concepts` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `enduring concepts` only if they sharpen the boundary of `enduring concepts` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `enduring concepts` should refuse.
- Bottom line: `enduring concepts` belongs in the repo only if `enduring concepts` keeps improving judgment after the historical costume is removed.

### modern transfer limits — Definition

- What this names: within `What survives, what has been revised, and how to transfer Freud without pseudo-clinical overreach`, `modern transfer limits` points to a concrete node rather than a prestige label.
- Object boundary: treat `modern transfer limits` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `modern transfer limits` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `modern transfer limits` before pulling it into a modern analogy.
- Skill value: `modern transfer limits` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `modern transfer limits` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `modern transfer limits` clearly enough that a later answer can say what `modern transfer limits` includes and excludes.

### modern transfer limits — Historical Context

- Historical location: ask when `modern transfer limits` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `modern transfer limits` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `modern transfer limits` says, but why `modern transfer limits` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `modern transfer limits` from later packaging or selective reuse.
- Reading move: map `modern transfer limits` against the timeline before treating `modern transfer limits` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `modern transfer limits` before extracting any lesson from `modern transfer limits`.
- Failure risk: without context, `modern transfer limits` can look cleaner, more universal, and more innocent than it actually was.

### modern transfer limits — Internal Tension

- Main tension: `modern transfer limits` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `modern transfer limits` makes possible and what `modern transfer limits` quietly suppresses at the same time.
- Interpretive value: the tension in `modern transfer limits` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `modern transfer limits` double-sided rather than converting `modern transfer limits` into moral reassurance.
- Research warning: if every sentence about `modern transfer limits` sounds admiring or dismissive, `modern transfer limits` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `modern transfer limits`.
- Better question: what does `modern transfer limits` solve, and what new silence does `modern transfer limits` create?

### modern transfer limits — Key Distinction

- Core distinction: separate `modern transfer limits` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `modern transfer limits`.
- Typical confusion: readers often mistake `modern transfer limits` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `modern transfer limits` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `modern transfer limits`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `modern transfer limits` by accident.
- Operational cue: define what `modern transfer limits` is not before claiming to know what `modern transfer limits` is.

### modern transfer limits — Method

- Method question: how does `modern transfer limits` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `modern transfer limits` rather than inspirational phrasing.
- Research use: `modern transfer limits` matters when `modern transfer limits` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `modern transfer limits`, not costume around `modern transfer limits`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `modern transfer limits`.
- Modern move: only transfer the mechanism in `modern transfer limits` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `modern transfer limits` without reproducing the method inside `modern transfer limits`.

### modern transfer limits — Operational Use

- Operational role: `modern transfer limits` belongs in the skill because `modern transfer limits` changes how prompts are framed and how problems are cut.
- Decision value: `modern transfer limits` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `modern transfer limits` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `modern transfer limits` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `modern transfer limits` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `modern transfer limits` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `modern transfer limits` make me check before I act?

### modern transfer limits — Transfer To Modern Problems

- Transfer principle: bring `modern transfer limits` forward only when the structural analogy around `modern transfer limits` is stronger than the historical costume.
- Modern fit: `modern transfer limits` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `modern transfer limits` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `modern transfer limits`, not the surrounding mythology around `modern transfer limits`.
- Bad transfer: using `modern transfer limits` to justify positions `modern transfer limits` was never built to adjudicate.
- Practical test: if the analogy for `modern transfer limits` cannot survive plainer language, `modern transfer limits` probably does not belong in the answer.
- Design implication: every modern use of `modern transfer limits` should make the analogy explicit instead of pretending `modern transfer limits` is self-evident.

### modern transfer limits — Failure Mode

- Failure pattern: `modern transfer limits` goes bad when `modern transfer limits` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `modern transfer limits` for authority, mood, or aesthetic and lose the underlying discipline in `modern transfer limits`.
- Research signal: when `modern transfer limits` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `modern transfer limits`.
- User risk: answers can start sounding impressive while `modern transfer limits` becomes less falsifiable.
- Repository risk: `modern transfer limits` can silently drift from operating concept into mascot.
- Correction move: pull `modern transfer limits` back to evidence, mechanism, and concrete limits.
- Best warning label: `modern transfer limits` should increase rigor, not lower the burden of proof.

### modern transfer limits — Common Misreading

- Typical misreading: `modern transfer limits` is often taken as a broad worldview when `modern transfer limits` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `modern transfer limits`.
- Consequence: readers then import claims into `modern transfer limits` that the corpus does not actually support for `modern transfer limits`.
- Skill response: keep `modern transfer limits` attached to the texts, cases, and institutional situations that make `modern transfer limits` legible.
- Modern correction: show what `modern transfer limits` can illuminate without pretending `modern transfer limits` answers everything nearby.
- Language fix: name the overreach around `modern transfer limits` directly instead of just gesturing at nuance.
- Reader test: if `modern transfer limits` suddenly explains every problem, `modern transfer limits` is being misread.

### modern transfer limits — Boundary Condition

- Boundary condition: `modern transfer limits` only travels well when the receiving problem shares enough structure with the original one behind `modern transfer limits`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `modern transfer limits`.
- Ethical test: some uses of `modern transfer limits` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `modern transfer limits` into careless advice.
- Good limit: it is fine for `modern transfer limits` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `modern transfer limits` has no boundary because `modern transfer limits` feels generically wise.
- Operational question: under what conditions should `modern transfer limits` be left unused?

### modern transfer limits — Better Question

- Better question: instead of asking whether `modern transfer limits` is simply true, ask what problem `modern transfer limits` was trying to solve.
- Reader upgrade: this turns `modern transfer limits` from a label into a live diagnostic instrument.
- Research upgrade: `modern transfer limits` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `modern transfer limits` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `modern transfer limits` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `modern transfer limits` becomes useful when `modern transfer limits` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `modern transfer limits` is framed as a problem-solving move?

### modern transfer limits — Example Move

- Example move: use `modern transfer limits` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `modern transfer limits` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `modern transfer limits` is strongest when `modern transfer limits` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `modern transfer limits` lets the user do that they could not do before.
- Transfer discipline: keep the move around `modern transfer limits` small and explicit so the analogy does not sprawl.
- Repository standard: if `modern transfer limits` never produces a concrete move, `modern transfer limits` is not yet operational.
- User question: what is one thing `modern transfer limits` would make me inspect differently right now?

### modern transfer limits — What To Preserve

- Preserve the pressure: `modern transfer limits` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `modern transfer limits` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `modern transfer limits`.
- Preserve the routing value: `modern transfer limits` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `modern transfer limits` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `modern transfer limits` stops is part of preserving `modern transfer limits` well.
- Preserve the usefulness: a transferred `modern transfer limits` should still help someone make a cleaner judgment.

### modern transfer limits — What To Drop

- Drop the costume: `modern transfer limits` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `modern transfer limits` has been understood.
- Drop the universal claim: `modern transfer limits` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `modern transfer limits` survives only as a catchphrase, `modern transfer limits` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `modern transfer limits` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `modern transfer limits` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `modern transfer limits` and remove the surrounding noise.

### modern transfer limits — Design Implication

- Design implication: `modern transfer limits` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `modern transfer limits` that later edits can stay honest.
- UX implication: users should feel `modern transfer limits` changing the angle of analysis, not just the tone.
- Evaluation implication: test `modern transfer limits` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `modern transfer limits` only if they sharpen the boundary of `modern transfer limits` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `modern transfer limits` should refuse.
- Bottom line: `modern transfer limits` belongs in the repo only if `modern transfer limits` keeps improving judgment after the historical costume is removed.

## 原始素材文件

这份 research 直接回链到本仓库的原始素材层：

- books：[interpretation-of-dreams.md](../sources/books/interpretation-of-dreams.md)、[introductory-lectures.md](../sources/books/introductory-lectures.md)、[psychopathology-of-everyday-life.md](../sources/books/psychopathology-of-everyday-life.md)
- transcripts：（无）
- articles：[britannica-freud.md](../sources/articles/britannica-freud.md)、[sep-freud.md](../sources/articles/sep-freud.md)
