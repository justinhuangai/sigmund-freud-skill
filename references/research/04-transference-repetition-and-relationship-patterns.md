# 移情、重复与关系模式 / Transference, Repetition, and Relationship Patterns

Why old relational scripts recur and how transference patterns show up in present life.

## Why This File Exists

- This file supports the Sigmund Freud person skill by making the research layer thick enough to guide real reasoning rather than surface style.
- It treats 弗洛伊德 as a structured cognitive system, not as a quotation source or prestige label.
- The focus of this file is Why old relational scripts recur and how transference patterns show up in present life..
- The notes below are organized to keep historical grounding, operational transfer, and misuse boundaries visible at the same time.

## Source Orientation

- Read the primary texts or documentary anchors first, then map them to later interpretation.
- Keep biography, corpus boundary, and later myth separate from direct conceptual claims.
- When transferring this material into a modern skill, preserve the structure and drop the historical costume.

## Research Notes

### transference — Definition

- What this names: within `Why old relational scripts recur and how transference patterns show up in present life`, `transference` points to a concrete node rather than a prestige label.
- Object boundary: treat `transference` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `transference` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `transference` before pulling it into a modern analogy.
- Skill value: `transference` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `transference` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `transference` clearly enough that a later answer can say what `transference` includes and excludes.

### transference — Historical Context

- Historical location: ask when `transference` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `transference` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `transference` says, but why `transference` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `transference` from later packaging or selective reuse.
- Reading move: map `transference` against the timeline before treating `transference` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `transference` before extracting any lesson from `transference`.
- Failure risk: without context, `transference` can look cleaner, more universal, and more innocent than it actually was.

### transference — Internal Tension

- Main tension: `transference` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `transference` makes possible and what `transference` quietly suppresses at the same time.
- Interpretive value: the tension in `transference` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `transference` double-sided rather than converting `transference` into moral reassurance.
- Research warning: if every sentence about `transference` sounds admiring or dismissive, `transference` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `transference`.
- Better question: what does `transference` solve, and what new silence does `transference` create?

### transference — Key Distinction

- Core distinction: separate `transference` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `transference`.
- Typical confusion: readers often mistake `transference` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `transference` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `transference`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `transference` by accident.
- Operational cue: define what `transference` is not before claiming to know what `transference` is.

### transference — Method

- Method question: how does `transference` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `transference` rather than inspirational phrasing.
- Research use: `transference` matters when `transference` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `transference`, not costume around `transference`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `transference`.
- Modern move: only transfer the mechanism in `transference` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `transference` without reproducing the method inside `transference`.

### transference — Operational Use

- Operational role: `transference` belongs in the skill because `transference` changes how prompts are framed and how problems are cut.
- Decision value: `transference` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `transference` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `transference` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `transference` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `transference` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `transference` make me check before I act?

### transference — Transfer To Modern Problems

- Transfer principle: bring `transference` forward only when the structural analogy around `transference` is stronger than the historical costume.
- Modern fit: `transference` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `transference` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `transference`, not the surrounding mythology around `transference`.
- Bad transfer: using `transference` to justify positions `transference` was never built to adjudicate.
- Practical test: if the analogy for `transference` cannot survive plainer language, `transference` probably does not belong in the answer.
- Design implication: every modern use of `transference` should make the analogy explicit instead of pretending `transference` is self-evident.

### transference — Failure Mode

- Failure pattern: `transference` goes bad when `transference` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `transference` for authority, mood, or aesthetic and lose the underlying discipline in `transference`.
- Research signal: when `transference` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `transference`.
- User risk: answers can start sounding impressive while `transference` becomes less falsifiable.
- Repository risk: `transference` can silently drift from operating concept into mascot.
- Correction move: pull `transference` back to evidence, mechanism, and concrete limits.
- Best warning label: `transference` should increase rigor, not lower the burden of proof.

### transference — Common Misreading

- Typical misreading: `transference` is often taken as a broad worldview when `transference` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `transference`.
- Consequence: readers then import claims into `transference` that the corpus does not actually support for `transference`.
- Skill response: keep `transference` attached to the texts, cases, and institutional situations that make `transference` legible.
- Modern correction: show what `transference` can illuminate without pretending `transference` answers everything nearby.
- Language fix: name the overreach around `transference` directly instead of just gesturing at nuance.
- Reader test: if `transference` suddenly explains every problem, `transference` is being misread.

### transference — Boundary Condition

- Boundary condition: `transference` only travels well when the receiving problem shares enough structure with the original one behind `transference`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `transference`.
- Ethical test: some uses of `transference` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `transference` into careless advice.
- Good limit: it is fine for `transference` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `transference` has no boundary because `transference` feels generically wise.
- Operational question: under what conditions should `transference` be left unused?

### transference — Better Question

- Better question: instead of asking whether `transference` is simply true, ask what problem `transference` was trying to solve.
- Reader upgrade: this turns `transference` from a label into a live diagnostic instrument.
- Research upgrade: `transference` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `transference` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `transference` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `transference` becomes useful when `transference` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `transference` is framed as a problem-solving move?

### transference — Example Move

- Example move: use `transference` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `transference` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `transference` is strongest when `transference` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `transference` lets the user do that they could not do before.
- Transfer discipline: keep the move around `transference` small and explicit so the analogy does not sprawl.
- Repository standard: if `transference` never produces a concrete move, `transference` is not yet operational.
- User question: what is one thing `transference` would make me inspect differently right now?

### transference — What To Preserve

- Preserve the pressure: `transference` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `transference` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `transference`.
- Preserve the routing value: `transference` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `transference` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `transference` stops is part of preserving `transference` well.
- Preserve the usefulness: a transferred `transference` should still help someone make a cleaner judgment.

### transference — What To Drop

- Drop the costume: `transference` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `transference` has been understood.
- Drop the universal claim: `transference` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `transference` survives only as a catchphrase, `transference` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `transference` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `transference` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `transference` and remove the surrounding noise.

### transference — Design Implication

- Design implication: `transference` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `transference` that later edits can stay honest.
- UX implication: users should feel `transference` changing the angle of analysis, not just the tone.
- Evaluation implication: test `transference` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `transference` only if they sharpen the boundary of `transference` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `transference` should refuse.
- Bottom line: `transference` belongs in the repo only if `transference` keeps improving judgment after the historical costume is removed.

### repetition compulsion — Definition

- What this names: within `Why old relational scripts recur and how transference patterns show up in present life`, `repetition compulsion` points to a concrete node rather than a prestige label.
- Object boundary: treat `repetition compulsion` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `repetition compulsion` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `repetition compulsion` before pulling it into a modern analogy.
- Skill value: `repetition compulsion` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `repetition compulsion` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `repetition compulsion` clearly enough that a later answer can say what `repetition compulsion` includes and excludes.

### repetition compulsion — Historical Context

- Historical location: ask when `repetition compulsion` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `repetition compulsion` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `repetition compulsion` says, but why `repetition compulsion` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `repetition compulsion` from later packaging or selective reuse.
- Reading move: map `repetition compulsion` against the timeline before treating `repetition compulsion` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `repetition compulsion` before extracting any lesson from `repetition compulsion`.
- Failure risk: without context, `repetition compulsion` can look cleaner, more universal, and more innocent than it actually was.

### repetition compulsion — Internal Tension

- Main tension: `repetition compulsion` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `repetition compulsion` makes possible and what `repetition compulsion` quietly suppresses at the same time.
- Interpretive value: the tension in `repetition compulsion` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `repetition compulsion` double-sided rather than converting `repetition compulsion` into moral reassurance.
- Research warning: if every sentence about `repetition compulsion` sounds admiring or dismissive, `repetition compulsion` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `repetition compulsion`.
- Better question: what does `repetition compulsion` solve, and what new silence does `repetition compulsion` create?

### repetition compulsion — Key Distinction

- Core distinction: separate `repetition compulsion` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `repetition compulsion`.
- Typical confusion: readers often mistake `repetition compulsion` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `repetition compulsion` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `repetition compulsion`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `repetition compulsion` by accident.
- Operational cue: define what `repetition compulsion` is not before claiming to know what `repetition compulsion` is.

### repetition compulsion — Method

- Method question: how does `repetition compulsion` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `repetition compulsion` rather than inspirational phrasing.
- Research use: `repetition compulsion` matters when `repetition compulsion` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `repetition compulsion`, not costume around `repetition compulsion`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `repetition compulsion`.
- Modern move: only transfer the mechanism in `repetition compulsion` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `repetition compulsion` without reproducing the method inside `repetition compulsion`.

### repetition compulsion — Operational Use

- Operational role: `repetition compulsion` belongs in the skill because `repetition compulsion` changes how prompts are framed and how problems are cut.
- Decision value: `repetition compulsion` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `repetition compulsion` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `repetition compulsion` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `repetition compulsion` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `repetition compulsion` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `repetition compulsion` make me check before I act?

### repetition compulsion — Transfer To Modern Problems

- Transfer principle: bring `repetition compulsion` forward only when the structural analogy around `repetition compulsion` is stronger than the historical costume.
- Modern fit: `repetition compulsion` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `repetition compulsion` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `repetition compulsion`, not the surrounding mythology around `repetition compulsion`.
- Bad transfer: using `repetition compulsion` to justify positions `repetition compulsion` was never built to adjudicate.
- Practical test: if the analogy for `repetition compulsion` cannot survive plainer language, `repetition compulsion` probably does not belong in the answer.
- Design implication: every modern use of `repetition compulsion` should make the analogy explicit instead of pretending `repetition compulsion` is self-evident.

### repetition compulsion — Failure Mode

- Failure pattern: `repetition compulsion` goes bad when `repetition compulsion` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `repetition compulsion` for authority, mood, or aesthetic and lose the underlying discipline in `repetition compulsion`.
- Research signal: when `repetition compulsion` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `repetition compulsion`.
- User risk: answers can start sounding impressive while `repetition compulsion` becomes less falsifiable.
- Repository risk: `repetition compulsion` can silently drift from operating concept into mascot.
- Correction move: pull `repetition compulsion` back to evidence, mechanism, and concrete limits.
- Best warning label: `repetition compulsion` should increase rigor, not lower the burden of proof.

### repetition compulsion — Common Misreading

- Typical misreading: `repetition compulsion` is often taken as a broad worldview when `repetition compulsion` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `repetition compulsion`.
- Consequence: readers then import claims into `repetition compulsion` that the corpus does not actually support for `repetition compulsion`.
- Skill response: keep `repetition compulsion` attached to the texts, cases, and institutional situations that make `repetition compulsion` legible.
- Modern correction: show what `repetition compulsion` can illuminate without pretending `repetition compulsion` answers everything nearby.
- Language fix: name the overreach around `repetition compulsion` directly instead of just gesturing at nuance.
- Reader test: if `repetition compulsion` suddenly explains every problem, `repetition compulsion` is being misread.

### repetition compulsion — Boundary Condition

- Boundary condition: `repetition compulsion` only travels well when the receiving problem shares enough structure with the original one behind `repetition compulsion`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `repetition compulsion`.
- Ethical test: some uses of `repetition compulsion` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `repetition compulsion` into careless advice.
- Good limit: it is fine for `repetition compulsion` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `repetition compulsion` has no boundary because `repetition compulsion` feels generically wise.
- Operational question: under what conditions should `repetition compulsion` be left unused?

### repetition compulsion — Better Question

- Better question: instead of asking whether `repetition compulsion` is simply true, ask what problem `repetition compulsion` was trying to solve.
- Reader upgrade: this turns `repetition compulsion` from a label into a live diagnostic instrument.
- Research upgrade: `repetition compulsion` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `repetition compulsion` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `repetition compulsion` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `repetition compulsion` becomes useful when `repetition compulsion` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `repetition compulsion` is framed as a problem-solving move?

### repetition compulsion — Example Move

- Example move: use `repetition compulsion` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `repetition compulsion` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `repetition compulsion` is strongest when `repetition compulsion` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `repetition compulsion` lets the user do that they could not do before.
- Transfer discipline: keep the move around `repetition compulsion` small and explicit so the analogy does not sprawl.
- Repository standard: if `repetition compulsion` never produces a concrete move, `repetition compulsion` is not yet operational.
- User question: what is one thing `repetition compulsion` would make me inspect differently right now?

### repetition compulsion — What To Preserve

- Preserve the pressure: `repetition compulsion` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `repetition compulsion` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `repetition compulsion`.
- Preserve the routing value: `repetition compulsion` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `repetition compulsion` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `repetition compulsion` stops is part of preserving `repetition compulsion` well.
- Preserve the usefulness: a transferred `repetition compulsion` should still help someone make a cleaner judgment.

### repetition compulsion — What To Drop

- Drop the costume: `repetition compulsion` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `repetition compulsion` has been understood.
- Drop the universal claim: `repetition compulsion` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `repetition compulsion` survives only as a catchphrase, `repetition compulsion` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `repetition compulsion` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `repetition compulsion` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `repetition compulsion` and remove the surrounding noise.

### repetition compulsion — Design Implication

- Design implication: `repetition compulsion` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `repetition compulsion` that later edits can stay honest.
- UX implication: users should feel `repetition compulsion` changing the angle of analysis, not just the tone.
- Evaluation implication: test `repetition compulsion` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `repetition compulsion` only if they sharpen the boundary of `repetition compulsion` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `repetition compulsion` should refuse.
- Bottom line: `repetition compulsion` belongs in the repo only if `repetition compulsion` keeps improving judgment after the historical costume is removed.

### working through — Definition

- What this names: within `Why old relational scripts recur and how transference patterns show up in present life`, `working through` points to a concrete node rather than a prestige label.
- Object boundary: treat `working through` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `working through` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `working through` before pulling it into a modern analogy.
- Skill value: `working through` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `working through` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `working through` clearly enough that a later answer can say what `working through` includes and excludes.

### working through — Historical Context

- Historical location: ask when `working through` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `working through` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `working through` says, but why `working through` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `working through` from later packaging or selective reuse.
- Reading move: map `working through` against the timeline before treating `working through` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `working through` before extracting any lesson from `working through`.
- Failure risk: without context, `working through` can look cleaner, more universal, and more innocent than it actually was.

### working through — Internal Tension

- Main tension: `working through` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `working through` makes possible and what `working through` quietly suppresses at the same time.
- Interpretive value: the tension in `working through` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `working through` double-sided rather than converting `working through` into moral reassurance.
- Research warning: if every sentence about `working through` sounds admiring or dismissive, `working through` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `working through`.
- Better question: what does `working through` solve, and what new silence does `working through` create?

### working through — Key Distinction

- Core distinction: separate `working through` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `working through`.
- Typical confusion: readers often mistake `working through` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `working through` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `working through`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `working through` by accident.
- Operational cue: define what `working through` is not before claiming to know what `working through` is.

### working through — Method

- Method question: how does `working through` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `working through` rather than inspirational phrasing.
- Research use: `working through` matters when `working through` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `working through`, not costume around `working through`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `working through`.
- Modern move: only transfer the mechanism in `working through` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `working through` without reproducing the method inside `working through`.

### working through — Operational Use

- Operational role: `working through` belongs in the skill because `working through` changes how prompts are framed and how problems are cut.
- Decision value: `working through` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `working through` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `working through` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `working through` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `working through` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `working through` make me check before I act?

### working through — Transfer To Modern Problems

- Transfer principle: bring `working through` forward only when the structural analogy around `working through` is stronger than the historical costume.
- Modern fit: `working through` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `working through` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `working through`, not the surrounding mythology around `working through`.
- Bad transfer: using `working through` to justify positions `working through` was never built to adjudicate.
- Practical test: if the analogy for `working through` cannot survive plainer language, `working through` probably does not belong in the answer.
- Design implication: every modern use of `working through` should make the analogy explicit instead of pretending `working through` is self-evident.

### working through — Failure Mode

- Failure pattern: `working through` goes bad when `working through` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `working through` for authority, mood, or aesthetic and lose the underlying discipline in `working through`.
- Research signal: when `working through` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `working through`.
- User risk: answers can start sounding impressive while `working through` becomes less falsifiable.
- Repository risk: `working through` can silently drift from operating concept into mascot.
- Correction move: pull `working through` back to evidence, mechanism, and concrete limits.
- Best warning label: `working through` should increase rigor, not lower the burden of proof.

### working through — Common Misreading

- Typical misreading: `working through` is often taken as a broad worldview when `working through` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `working through`.
- Consequence: readers then import claims into `working through` that the corpus does not actually support for `working through`.
- Skill response: keep `working through` attached to the texts, cases, and institutional situations that make `working through` legible.
- Modern correction: show what `working through` can illuminate without pretending `working through` answers everything nearby.
- Language fix: name the overreach around `working through` directly instead of just gesturing at nuance.
- Reader test: if `working through` suddenly explains every problem, `working through` is being misread.

### working through — Boundary Condition

- Boundary condition: `working through` only travels well when the receiving problem shares enough structure with the original one behind `working through`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `working through`.
- Ethical test: some uses of `working through` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `working through` into careless advice.
- Good limit: it is fine for `working through` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `working through` has no boundary because `working through` feels generically wise.
- Operational question: under what conditions should `working through` be left unused?

### working through — Better Question

- Better question: instead of asking whether `working through` is simply true, ask what problem `working through` was trying to solve.
- Reader upgrade: this turns `working through` from a label into a live diagnostic instrument.
- Research upgrade: `working through` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `working through` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `working through` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `working through` becomes useful when `working through` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `working through` is framed as a problem-solving move?

### working through — Example Move

- Example move: use `working through` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `working through` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `working through` is strongest when `working through` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `working through` lets the user do that they could not do before.
- Transfer discipline: keep the move around `working through` small and explicit so the analogy does not sprawl.
- Repository standard: if `working through` never produces a concrete move, `working through` is not yet operational.
- User question: what is one thing `working through` would make me inspect differently right now?

### working through — What To Preserve

- Preserve the pressure: `working through` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `working through` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `working through`.
- Preserve the routing value: `working through` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `working through` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `working through` stops is part of preserving `working through` well.
- Preserve the usefulness: a transferred `working through` should still help someone make a cleaner judgment.

### working through — What To Drop

- Drop the costume: `working through` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `working through` has been understood.
- Drop the universal claim: `working through` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `working through` survives only as a catchphrase, `working through` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `working through` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `working through` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `working through` and remove the surrounding noise.

### working through — Design Implication

- Design implication: `working through` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `working through` that later edits can stay honest.
- UX implication: users should feel `working through` changing the angle of analysis, not just the tone.
- Evaluation implication: test `working through` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `working through` only if they sharpen the boundary of `working through` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `working through` should refuse.
- Bottom line: `working through` belongs in the repo only if `working through` keeps improving judgment after the historical costume is removed.

### ambivalence in attachment — Definition

- What this names: within `Why old relational scripts recur and how transference patterns show up in present life`, `ambivalence in attachment` points to a concrete node rather than a prestige label.
- Object boundary: treat `ambivalence in attachment` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `ambivalence in attachment` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `ambivalence in attachment` before pulling it into a modern analogy.
- Skill value: `ambivalence in attachment` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `ambivalence in attachment` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `ambivalence in attachment` clearly enough that a later answer can say what `ambivalence in attachment` includes and excludes.

### ambivalence in attachment — Historical Context

- Historical location: ask when `ambivalence in attachment` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `ambivalence in attachment` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `ambivalence in attachment` says, but why `ambivalence in attachment` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `ambivalence in attachment` from later packaging or selective reuse.
- Reading move: map `ambivalence in attachment` against the timeline before treating `ambivalence in attachment` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `ambivalence in attachment` before extracting any lesson from `ambivalence in attachment`.
- Failure risk: without context, `ambivalence in attachment` can look cleaner, more universal, and more innocent than it actually was.

### ambivalence in attachment — Internal Tension

- Main tension: `ambivalence in attachment` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `ambivalence in attachment` makes possible and what `ambivalence in attachment` quietly suppresses at the same time.
- Interpretive value: the tension in `ambivalence in attachment` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `ambivalence in attachment` double-sided rather than converting `ambivalence in attachment` into moral reassurance.
- Research warning: if every sentence about `ambivalence in attachment` sounds admiring or dismissive, `ambivalence in attachment` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `ambivalence in attachment`.
- Better question: what does `ambivalence in attachment` solve, and what new silence does `ambivalence in attachment` create?

### ambivalence in attachment — Key Distinction

- Core distinction: separate `ambivalence in attachment` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `ambivalence in attachment`.
- Typical confusion: readers often mistake `ambivalence in attachment` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `ambivalence in attachment` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `ambivalence in attachment`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `ambivalence in attachment` by accident.
- Operational cue: define what `ambivalence in attachment` is not before claiming to know what `ambivalence in attachment` is.

### ambivalence in attachment — Method

- Method question: how does `ambivalence in attachment` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `ambivalence in attachment` rather than inspirational phrasing.
- Research use: `ambivalence in attachment` matters when `ambivalence in attachment` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `ambivalence in attachment`, not costume around `ambivalence in attachment`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `ambivalence in attachment`.
- Modern move: only transfer the mechanism in `ambivalence in attachment` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `ambivalence in attachment` without reproducing the method inside `ambivalence in attachment`.

### ambivalence in attachment — Operational Use

- Operational role: `ambivalence in attachment` belongs in the skill because `ambivalence in attachment` changes how prompts are framed and how problems are cut.
- Decision value: `ambivalence in attachment` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `ambivalence in attachment` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `ambivalence in attachment` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `ambivalence in attachment` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `ambivalence in attachment` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `ambivalence in attachment` make me check before I act?

### ambivalence in attachment — Transfer To Modern Problems

- Transfer principle: bring `ambivalence in attachment` forward only when the structural analogy around `ambivalence in attachment` is stronger than the historical costume.
- Modern fit: `ambivalence in attachment` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `ambivalence in attachment` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `ambivalence in attachment`, not the surrounding mythology around `ambivalence in attachment`.
- Bad transfer: using `ambivalence in attachment` to justify positions `ambivalence in attachment` was never built to adjudicate.
- Practical test: if the analogy for `ambivalence in attachment` cannot survive plainer language, `ambivalence in attachment` probably does not belong in the answer.
- Design implication: every modern use of `ambivalence in attachment` should make the analogy explicit instead of pretending `ambivalence in attachment` is self-evident.

### ambivalence in attachment — Failure Mode

- Failure pattern: `ambivalence in attachment` goes bad when `ambivalence in attachment` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `ambivalence in attachment` for authority, mood, or aesthetic and lose the underlying discipline in `ambivalence in attachment`.
- Research signal: when `ambivalence in attachment` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `ambivalence in attachment`.
- User risk: answers can start sounding impressive while `ambivalence in attachment` becomes less falsifiable.
- Repository risk: `ambivalence in attachment` can silently drift from operating concept into mascot.
- Correction move: pull `ambivalence in attachment` back to evidence, mechanism, and concrete limits.
- Best warning label: `ambivalence in attachment` should increase rigor, not lower the burden of proof.

### ambivalence in attachment — Common Misreading

- Typical misreading: `ambivalence in attachment` is often taken as a broad worldview when `ambivalence in attachment` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `ambivalence in attachment`.
- Consequence: readers then import claims into `ambivalence in attachment` that the corpus does not actually support for `ambivalence in attachment`.
- Skill response: keep `ambivalence in attachment` attached to the texts, cases, and institutional situations that make `ambivalence in attachment` legible.
- Modern correction: show what `ambivalence in attachment` can illuminate without pretending `ambivalence in attachment` answers everything nearby.
- Language fix: name the overreach around `ambivalence in attachment` directly instead of just gesturing at nuance.
- Reader test: if `ambivalence in attachment` suddenly explains every problem, `ambivalence in attachment` is being misread.

### ambivalence in attachment — Boundary Condition

- Boundary condition: `ambivalence in attachment` only travels well when the receiving problem shares enough structure with the original one behind `ambivalence in attachment`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `ambivalence in attachment`.
- Ethical test: some uses of `ambivalence in attachment` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `ambivalence in attachment` into careless advice.
- Good limit: it is fine for `ambivalence in attachment` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `ambivalence in attachment` has no boundary because `ambivalence in attachment` feels generically wise.
- Operational question: under what conditions should `ambivalence in attachment` be left unused?

### ambivalence in attachment — Better Question

- Better question: instead of asking whether `ambivalence in attachment` is simply true, ask what problem `ambivalence in attachment` was trying to solve.
- Reader upgrade: this turns `ambivalence in attachment` from a label into a live diagnostic instrument.
- Research upgrade: `ambivalence in attachment` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `ambivalence in attachment` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `ambivalence in attachment` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `ambivalence in attachment` becomes useful when `ambivalence in attachment` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `ambivalence in attachment` is framed as a problem-solving move?

### ambivalence in attachment — Example Move

- Example move: use `ambivalence in attachment` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `ambivalence in attachment` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `ambivalence in attachment` is strongest when `ambivalence in attachment` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `ambivalence in attachment` lets the user do that they could not do before.
- Transfer discipline: keep the move around `ambivalence in attachment` small and explicit so the analogy does not sprawl.
- Repository standard: if `ambivalence in attachment` never produces a concrete move, `ambivalence in attachment` is not yet operational.
- User question: what is one thing `ambivalence in attachment` would make me inspect differently right now?

### ambivalence in attachment — What To Preserve

- Preserve the pressure: `ambivalence in attachment` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `ambivalence in attachment` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `ambivalence in attachment`.
- Preserve the routing value: `ambivalence in attachment` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `ambivalence in attachment` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `ambivalence in attachment` stops is part of preserving `ambivalence in attachment` well.
- Preserve the usefulness: a transferred `ambivalence in attachment` should still help someone make a cleaner judgment.

### ambivalence in attachment — What To Drop

- Drop the costume: `ambivalence in attachment` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `ambivalence in attachment` has been understood.
- Drop the universal claim: `ambivalence in attachment` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `ambivalence in attachment` survives only as a catchphrase, `ambivalence in attachment` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `ambivalence in attachment` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `ambivalence in attachment` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `ambivalence in attachment` and remove the surrounding noise.

### ambivalence in attachment — Design Implication

- Design implication: `ambivalence in attachment` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `ambivalence in attachment` that later edits can stay honest.
- UX implication: users should feel `ambivalence in attachment` changing the angle of analysis, not just the tone.
- Evaluation implication: test `ambivalence in attachment` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `ambivalence in attachment` only if they sharpen the boundary of `ambivalence in attachment` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `ambivalence in attachment` should refuse.
- Bottom line: `ambivalence in attachment` belongs in the repo only if `ambivalence in attachment` keeps improving judgment after the historical costume is removed.

## 原始素材文件

这份 research 直接回链到本仓库的原始素材层：

- books：[interpretation-of-dreams.md](../sources/books/interpretation-of-dreams.md)、[introductory-lectures.md](../sources/books/introductory-lectures.md)、[psychopathology-of-everyday-life.md](../sources/books/psychopathology-of-everyday-life.md)
- transcripts：（无）
- articles：[britannica-freud.md](../sources/articles/britannica-freud.md)、[sep-freud.md](../sources/articles/sep-freud.md)
