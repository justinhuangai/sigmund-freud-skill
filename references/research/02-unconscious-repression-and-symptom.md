# 无意识、压抑与症状 / Unconscious, Repression, and Symptom

The unconscious, repression, symptom formation, and why indirect expression matters.

## Why This File Exists

- This file supports the Sigmund Freud person skill by making the research layer thick enough to guide real reasoning rather than surface style.
- It treats 弗洛伊德 as a structured cognitive system, not as a quotation source or prestige label.
- The focus of this file is The unconscious, repression, symptom formation, and why indirect expression matters..
- The notes below are organized to keep historical grounding, operational transfer, and misuse boundaries visible at the same time.

## Source Orientation

- Read the primary texts or documentary anchors first, then map them to later interpretation.
- Keep biography, corpus boundary, and later myth separate from direct conceptual claims.
- When transferring this material into a modern skill, preserve the structure and drop the historical costume.

## Research Notes

### unconscious wish — Definition

- What this names: within `The unconscious, repression, symptom formation, and why indirect expression matters`, `unconscious wish` points to a concrete node rather than a prestige label.
- Object boundary: treat `unconscious wish` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `unconscious wish` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `unconscious wish` before pulling it into a modern analogy.
- Skill value: `unconscious wish` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `unconscious wish` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `unconscious wish` clearly enough that a later answer can say what `unconscious wish` includes and excludes.

### unconscious wish — Historical Context

- Historical location: ask when `unconscious wish` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `unconscious wish` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `unconscious wish` says, but why `unconscious wish` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `unconscious wish` from later packaging or selective reuse.
- Reading move: map `unconscious wish` against the timeline before treating `unconscious wish` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `unconscious wish` before extracting any lesson from `unconscious wish`.
- Failure risk: without context, `unconscious wish` can look cleaner, more universal, and more innocent than it actually was.

### unconscious wish — Internal Tension

- Main tension: `unconscious wish` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `unconscious wish` makes possible and what `unconscious wish` quietly suppresses at the same time.
- Interpretive value: the tension in `unconscious wish` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `unconscious wish` double-sided rather than converting `unconscious wish` into moral reassurance.
- Research warning: if every sentence about `unconscious wish` sounds admiring or dismissive, `unconscious wish` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `unconscious wish`.
- Better question: what does `unconscious wish` solve, and what new silence does `unconscious wish` create?

### unconscious wish — Key Distinction

- Core distinction: separate `unconscious wish` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `unconscious wish`.
- Typical confusion: readers often mistake `unconscious wish` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `unconscious wish` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `unconscious wish`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `unconscious wish` by accident.
- Operational cue: define what `unconscious wish` is not before claiming to know what `unconscious wish` is.

### unconscious wish — Method

- Method question: how does `unconscious wish` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `unconscious wish` rather than inspirational phrasing.
- Research use: `unconscious wish` matters when `unconscious wish` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `unconscious wish`, not costume around `unconscious wish`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `unconscious wish`.
- Modern move: only transfer the mechanism in `unconscious wish` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `unconscious wish` without reproducing the method inside `unconscious wish`.

### unconscious wish — Operational Use

- Operational role: `unconscious wish` belongs in the skill because `unconscious wish` changes how prompts are framed and how problems are cut.
- Decision value: `unconscious wish` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `unconscious wish` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `unconscious wish` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `unconscious wish` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `unconscious wish` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `unconscious wish` make me check before I act?

### unconscious wish — Transfer To Modern Problems

- Transfer principle: bring `unconscious wish` forward only when the structural analogy around `unconscious wish` is stronger than the historical costume.
- Modern fit: `unconscious wish` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `unconscious wish` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `unconscious wish`, not the surrounding mythology around `unconscious wish`.
- Bad transfer: using `unconscious wish` to justify positions `unconscious wish` was never built to adjudicate.
- Practical test: if the analogy for `unconscious wish` cannot survive plainer language, `unconscious wish` probably does not belong in the answer.
- Design implication: every modern use of `unconscious wish` should make the analogy explicit instead of pretending `unconscious wish` is self-evident.

### unconscious wish — Failure Mode

- Failure pattern: `unconscious wish` goes bad when `unconscious wish` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `unconscious wish` for authority, mood, or aesthetic and lose the underlying discipline in `unconscious wish`.
- Research signal: when `unconscious wish` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `unconscious wish`.
- User risk: answers can start sounding impressive while `unconscious wish` becomes less falsifiable.
- Repository risk: `unconscious wish` can silently drift from operating concept into mascot.
- Correction move: pull `unconscious wish` back to evidence, mechanism, and concrete limits.
- Best warning label: `unconscious wish` should increase rigor, not lower the burden of proof.

### unconscious wish — Common Misreading

- Typical misreading: `unconscious wish` is often taken as a broad worldview when `unconscious wish` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `unconscious wish`.
- Consequence: readers then import claims into `unconscious wish` that the corpus does not actually support for `unconscious wish`.
- Skill response: keep `unconscious wish` attached to the texts, cases, and institutional situations that make `unconscious wish` legible.
- Modern correction: show what `unconscious wish` can illuminate without pretending `unconscious wish` answers everything nearby.
- Language fix: name the overreach around `unconscious wish` directly instead of just gesturing at nuance.
- Reader test: if `unconscious wish` suddenly explains every problem, `unconscious wish` is being misread.

### unconscious wish — Boundary Condition

- Boundary condition: `unconscious wish` only travels well when the receiving problem shares enough structure with the original one behind `unconscious wish`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `unconscious wish`.
- Ethical test: some uses of `unconscious wish` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `unconscious wish` into careless advice.
- Good limit: it is fine for `unconscious wish` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `unconscious wish` has no boundary because `unconscious wish` feels generically wise.
- Operational question: under what conditions should `unconscious wish` be left unused?

### unconscious wish — Better Question

- Better question: instead of asking whether `unconscious wish` is simply true, ask what problem `unconscious wish` was trying to solve.
- Reader upgrade: this turns `unconscious wish` from a label into a live diagnostic instrument.
- Research upgrade: `unconscious wish` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `unconscious wish` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `unconscious wish` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `unconscious wish` becomes useful when `unconscious wish` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `unconscious wish` is framed as a problem-solving move?

### unconscious wish — Example Move

- Example move: use `unconscious wish` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `unconscious wish` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `unconscious wish` is strongest when `unconscious wish` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `unconscious wish` lets the user do that they could not do before.
- Transfer discipline: keep the move around `unconscious wish` small and explicit so the analogy does not sprawl.
- Repository standard: if `unconscious wish` never produces a concrete move, `unconscious wish` is not yet operational.
- User question: what is one thing `unconscious wish` would make me inspect differently right now?

### unconscious wish — What To Preserve

- Preserve the pressure: `unconscious wish` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `unconscious wish` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `unconscious wish`.
- Preserve the routing value: `unconscious wish` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `unconscious wish` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `unconscious wish` stops is part of preserving `unconscious wish` well.
- Preserve the usefulness: a transferred `unconscious wish` should still help someone make a cleaner judgment.

### unconscious wish — What To Drop

- Drop the costume: `unconscious wish` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `unconscious wish` has been understood.
- Drop the universal claim: `unconscious wish` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `unconscious wish` survives only as a catchphrase, `unconscious wish` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `unconscious wish` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `unconscious wish` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `unconscious wish` and remove the surrounding noise.

### unconscious wish — Design Implication

- Design implication: `unconscious wish` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `unconscious wish` that later edits can stay honest.
- UX implication: users should feel `unconscious wish` changing the angle of analysis, not just the tone.
- Evaluation implication: test `unconscious wish` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `unconscious wish` only if they sharpen the boundary of `unconscious wish` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `unconscious wish` should refuse.
- Bottom line: `unconscious wish` belongs in the repo only if `unconscious wish` keeps improving judgment after the historical costume is removed.

### repression — Definition

- What this names: within `The unconscious, repression, symptom formation, and why indirect expression matters`, `repression` points to a concrete node rather than a prestige label.
- Object boundary: treat `repression` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `repression` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `repression` before pulling it into a modern analogy.
- Skill value: `repression` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `repression` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `repression` clearly enough that a later answer can say what `repression` includes and excludes.

### repression — Historical Context

- Historical location: ask when `repression` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `repression` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `repression` says, but why `repression` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `repression` from later packaging or selective reuse.
- Reading move: map `repression` against the timeline before treating `repression` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `repression` before extracting any lesson from `repression`.
- Failure risk: without context, `repression` can look cleaner, more universal, and more innocent than it actually was.

### repression — Internal Tension

- Main tension: `repression` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `repression` makes possible and what `repression` quietly suppresses at the same time.
- Interpretive value: the tension in `repression` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `repression` double-sided rather than converting `repression` into moral reassurance.
- Research warning: if every sentence about `repression` sounds admiring or dismissive, `repression` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `repression`.
- Better question: what does `repression` solve, and what new silence does `repression` create?

### repression — Key Distinction

- Core distinction: separate `repression` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `repression`.
- Typical confusion: readers often mistake `repression` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `repression` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `repression`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `repression` by accident.
- Operational cue: define what `repression` is not before claiming to know what `repression` is.

### repression — Method

- Method question: how does `repression` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `repression` rather than inspirational phrasing.
- Research use: `repression` matters when `repression` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `repression`, not costume around `repression`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `repression`.
- Modern move: only transfer the mechanism in `repression` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `repression` without reproducing the method inside `repression`.

### repression — Operational Use

- Operational role: `repression` belongs in the skill because `repression` changes how prompts are framed and how problems are cut.
- Decision value: `repression` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `repression` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `repression` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `repression` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `repression` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `repression` make me check before I act?

### repression — Transfer To Modern Problems

- Transfer principle: bring `repression` forward only when the structural analogy around `repression` is stronger than the historical costume.
- Modern fit: `repression` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `repression` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `repression`, not the surrounding mythology around `repression`.
- Bad transfer: using `repression` to justify positions `repression` was never built to adjudicate.
- Practical test: if the analogy for `repression` cannot survive plainer language, `repression` probably does not belong in the answer.
- Design implication: every modern use of `repression` should make the analogy explicit instead of pretending `repression` is self-evident.

### repression — Failure Mode

- Failure pattern: `repression` goes bad when `repression` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `repression` for authority, mood, or aesthetic and lose the underlying discipline in `repression`.
- Research signal: when `repression` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `repression`.
- User risk: answers can start sounding impressive while `repression` becomes less falsifiable.
- Repository risk: `repression` can silently drift from operating concept into mascot.
- Correction move: pull `repression` back to evidence, mechanism, and concrete limits.
- Best warning label: `repression` should increase rigor, not lower the burden of proof.

### repression — Common Misreading

- Typical misreading: `repression` is often taken as a broad worldview when `repression` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `repression`.
- Consequence: readers then import claims into `repression` that the corpus does not actually support for `repression`.
- Skill response: keep `repression` attached to the texts, cases, and institutional situations that make `repression` legible.
- Modern correction: show what `repression` can illuminate without pretending `repression` answers everything nearby.
- Language fix: name the overreach around `repression` directly instead of just gesturing at nuance.
- Reader test: if `repression` suddenly explains every problem, `repression` is being misread.

### repression — Boundary Condition

- Boundary condition: `repression` only travels well when the receiving problem shares enough structure with the original one behind `repression`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `repression`.
- Ethical test: some uses of `repression` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `repression` into careless advice.
- Good limit: it is fine for `repression` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `repression` has no boundary because `repression` feels generically wise.
- Operational question: under what conditions should `repression` be left unused?

### repression — Better Question

- Better question: instead of asking whether `repression` is simply true, ask what problem `repression` was trying to solve.
- Reader upgrade: this turns `repression` from a label into a live diagnostic instrument.
- Research upgrade: `repression` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `repression` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `repression` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `repression` becomes useful when `repression` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `repression` is framed as a problem-solving move?

### repression — Example Move

- Example move: use `repression` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `repression` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `repression` is strongest when `repression` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `repression` lets the user do that they could not do before.
- Transfer discipline: keep the move around `repression` small and explicit so the analogy does not sprawl.
- Repository standard: if `repression` never produces a concrete move, `repression` is not yet operational.
- User question: what is one thing `repression` would make me inspect differently right now?

### repression — What To Preserve

- Preserve the pressure: `repression` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `repression` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `repression`.
- Preserve the routing value: `repression` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `repression` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `repression` stops is part of preserving `repression` well.
- Preserve the usefulness: a transferred `repression` should still help someone make a cleaner judgment.

### repression — What To Drop

- Drop the costume: `repression` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `repression` has been understood.
- Drop the universal claim: `repression` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `repression` survives only as a catchphrase, `repression` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `repression` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `repression` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `repression` and remove the surrounding noise.

### repression — Design Implication

- Design implication: `repression` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `repression` that later edits can stay honest.
- UX implication: users should feel `repression` changing the angle of analysis, not just the tone.
- Evaluation implication: test `repression` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `repression` only if they sharpen the boundary of `repression` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `repression` should refuse.
- Bottom line: `repression` belongs in the repo only if `repression` keeps improving judgment after the historical costume is removed.

### symptom formation — Definition

- What this names: within `The unconscious, repression, symptom formation, and why indirect expression matters`, `symptom formation` points to a concrete node rather than a prestige label.
- Object boundary: treat `symptom formation` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `symptom formation` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `symptom formation` before pulling it into a modern analogy.
- Skill value: `symptom formation` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `symptom formation` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `symptom formation` clearly enough that a later answer can say what `symptom formation` includes and excludes.

### symptom formation — Historical Context

- Historical location: ask when `symptom formation` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `symptom formation` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `symptom formation` says, but why `symptom formation` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `symptom formation` from later packaging or selective reuse.
- Reading move: map `symptom formation` against the timeline before treating `symptom formation` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `symptom formation` before extracting any lesson from `symptom formation`.
- Failure risk: without context, `symptom formation` can look cleaner, more universal, and more innocent than it actually was.

### symptom formation — Internal Tension

- Main tension: `symptom formation` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `symptom formation` makes possible and what `symptom formation` quietly suppresses at the same time.
- Interpretive value: the tension in `symptom formation` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `symptom formation` double-sided rather than converting `symptom formation` into moral reassurance.
- Research warning: if every sentence about `symptom formation` sounds admiring or dismissive, `symptom formation` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `symptom formation`.
- Better question: what does `symptom formation` solve, and what new silence does `symptom formation` create?

### symptom formation — Key Distinction

- Core distinction: separate `symptom formation` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `symptom formation`.
- Typical confusion: readers often mistake `symptom formation` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `symptom formation` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `symptom formation`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `symptom formation` by accident.
- Operational cue: define what `symptom formation` is not before claiming to know what `symptom formation` is.

### symptom formation — Method

- Method question: how does `symptom formation` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `symptom formation` rather than inspirational phrasing.
- Research use: `symptom formation` matters when `symptom formation` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `symptom formation`, not costume around `symptom formation`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `symptom formation`.
- Modern move: only transfer the mechanism in `symptom formation` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `symptom formation` without reproducing the method inside `symptom formation`.

### symptom formation — Operational Use

- Operational role: `symptom formation` belongs in the skill because `symptom formation` changes how prompts are framed and how problems are cut.
- Decision value: `symptom formation` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `symptom formation` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `symptom formation` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `symptom formation` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `symptom formation` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `symptom formation` make me check before I act?

### symptom formation — Transfer To Modern Problems

- Transfer principle: bring `symptom formation` forward only when the structural analogy around `symptom formation` is stronger than the historical costume.
- Modern fit: `symptom formation` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `symptom formation` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `symptom formation`, not the surrounding mythology around `symptom formation`.
- Bad transfer: using `symptom formation` to justify positions `symptom formation` was never built to adjudicate.
- Practical test: if the analogy for `symptom formation` cannot survive plainer language, `symptom formation` probably does not belong in the answer.
- Design implication: every modern use of `symptom formation` should make the analogy explicit instead of pretending `symptom formation` is self-evident.

### symptom formation — Failure Mode

- Failure pattern: `symptom formation` goes bad when `symptom formation` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `symptom formation` for authority, mood, or aesthetic and lose the underlying discipline in `symptom formation`.
- Research signal: when `symptom formation` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `symptom formation`.
- User risk: answers can start sounding impressive while `symptom formation` becomes less falsifiable.
- Repository risk: `symptom formation` can silently drift from operating concept into mascot.
- Correction move: pull `symptom formation` back to evidence, mechanism, and concrete limits.
- Best warning label: `symptom formation` should increase rigor, not lower the burden of proof.

### symptom formation — Common Misreading

- Typical misreading: `symptom formation` is often taken as a broad worldview when `symptom formation` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `symptom formation`.
- Consequence: readers then import claims into `symptom formation` that the corpus does not actually support for `symptom formation`.
- Skill response: keep `symptom formation` attached to the texts, cases, and institutional situations that make `symptom formation` legible.
- Modern correction: show what `symptom formation` can illuminate without pretending `symptom formation` answers everything nearby.
- Language fix: name the overreach around `symptom formation` directly instead of just gesturing at nuance.
- Reader test: if `symptom formation` suddenly explains every problem, `symptom formation` is being misread.

### symptom formation — Boundary Condition

- Boundary condition: `symptom formation` only travels well when the receiving problem shares enough structure with the original one behind `symptom formation`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `symptom formation`.
- Ethical test: some uses of `symptom formation` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `symptom formation` into careless advice.
- Good limit: it is fine for `symptom formation` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `symptom formation` has no boundary because `symptom formation` feels generically wise.
- Operational question: under what conditions should `symptom formation` be left unused?

### symptom formation — Better Question

- Better question: instead of asking whether `symptom formation` is simply true, ask what problem `symptom formation` was trying to solve.
- Reader upgrade: this turns `symptom formation` from a label into a live diagnostic instrument.
- Research upgrade: `symptom formation` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `symptom formation` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `symptom formation` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `symptom formation` becomes useful when `symptom formation` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `symptom formation` is framed as a problem-solving move?

### symptom formation — Example Move

- Example move: use `symptom formation` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `symptom formation` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `symptom formation` is strongest when `symptom formation` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `symptom formation` lets the user do that they could not do before.
- Transfer discipline: keep the move around `symptom formation` small and explicit so the analogy does not sprawl.
- Repository standard: if `symptom formation` never produces a concrete move, `symptom formation` is not yet operational.
- User question: what is one thing `symptom formation` would make me inspect differently right now?

### symptom formation — What To Preserve

- Preserve the pressure: `symptom formation` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `symptom formation` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `symptom formation`.
- Preserve the routing value: `symptom formation` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `symptom formation` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `symptom formation` stops is part of preserving `symptom formation` well.
- Preserve the usefulness: a transferred `symptom formation` should still help someone make a cleaner judgment.

### symptom formation — What To Drop

- Drop the costume: `symptom formation` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `symptom formation` has been understood.
- Drop the universal claim: `symptom formation` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `symptom formation` survives only as a catchphrase, `symptom formation` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `symptom formation` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `symptom formation` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `symptom formation` and remove the surrounding noise.

### symptom formation — Design Implication

- Design implication: `symptom formation` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `symptom formation` that later edits can stay honest.
- UX implication: users should feel `symptom formation` changing the angle of analysis, not just the tone.
- Evaluation implication: test `symptom formation` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `symptom formation` only if they sharpen the boundary of `symptom formation` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `symptom formation` should refuse.
- Bottom line: `symptom formation` belongs in the repo only if `symptom formation` keeps improving judgment after the historical costume is removed.

### return of the repressed — Definition

- What this names: within `The unconscious, repression, symptom formation, and why indirect expression matters`, `return of the repressed` points to a concrete node rather than a prestige label.
- Object boundary: treat `return of the repressed` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `return of the repressed` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `return of the repressed` before pulling it into a modern analogy.
- Skill value: `return of the repressed` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `return of the repressed` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `return of the repressed` clearly enough that a later answer can say what `return of the repressed` includes and excludes.

### return of the repressed — Historical Context

- Historical location: ask when `return of the repressed` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `return of the repressed` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `return of the repressed` says, but why `return of the repressed` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `return of the repressed` from later packaging or selective reuse.
- Reading move: map `return of the repressed` against the timeline before treating `return of the repressed` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `return of the repressed` before extracting any lesson from `return of the repressed`.
- Failure risk: without context, `return of the repressed` can look cleaner, more universal, and more innocent than it actually was.

### return of the repressed — Internal Tension

- Main tension: `return of the repressed` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `return of the repressed` makes possible and what `return of the repressed` quietly suppresses at the same time.
- Interpretive value: the tension in `return of the repressed` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `return of the repressed` double-sided rather than converting `return of the repressed` into moral reassurance.
- Research warning: if every sentence about `return of the repressed` sounds admiring or dismissive, `return of the repressed` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `return of the repressed`.
- Better question: what does `return of the repressed` solve, and what new silence does `return of the repressed` create?

### return of the repressed — Key Distinction

- Core distinction: separate `return of the repressed` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `return of the repressed`.
- Typical confusion: readers often mistake `return of the repressed` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `return of the repressed` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `return of the repressed`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `return of the repressed` by accident.
- Operational cue: define what `return of the repressed` is not before claiming to know what `return of the repressed` is.

### return of the repressed — Method

- Method question: how does `return of the repressed` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `return of the repressed` rather than inspirational phrasing.
- Research use: `return of the repressed` matters when `return of the repressed` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `return of the repressed`, not costume around `return of the repressed`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `return of the repressed`.
- Modern move: only transfer the mechanism in `return of the repressed` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `return of the repressed` without reproducing the method inside `return of the repressed`.

### return of the repressed — Operational Use

- Operational role: `return of the repressed` belongs in the skill because `return of the repressed` changes how prompts are framed and how problems are cut.
- Decision value: `return of the repressed` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `return of the repressed` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `return of the repressed` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `return of the repressed` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `return of the repressed` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `return of the repressed` make me check before I act?

### return of the repressed — Transfer To Modern Problems

- Transfer principle: bring `return of the repressed` forward only when the structural analogy around `return of the repressed` is stronger than the historical costume.
- Modern fit: `return of the repressed` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `return of the repressed` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `return of the repressed`, not the surrounding mythology around `return of the repressed`.
- Bad transfer: using `return of the repressed` to justify positions `return of the repressed` was never built to adjudicate.
- Practical test: if the analogy for `return of the repressed` cannot survive plainer language, `return of the repressed` probably does not belong in the answer.
- Design implication: every modern use of `return of the repressed` should make the analogy explicit instead of pretending `return of the repressed` is self-evident.

### return of the repressed — Failure Mode

- Failure pattern: `return of the repressed` goes bad when `return of the repressed` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `return of the repressed` for authority, mood, or aesthetic and lose the underlying discipline in `return of the repressed`.
- Research signal: when `return of the repressed` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `return of the repressed`.
- User risk: answers can start sounding impressive while `return of the repressed` becomes less falsifiable.
- Repository risk: `return of the repressed` can silently drift from operating concept into mascot.
- Correction move: pull `return of the repressed` back to evidence, mechanism, and concrete limits.
- Best warning label: `return of the repressed` should increase rigor, not lower the burden of proof.

### return of the repressed — Common Misreading

- Typical misreading: `return of the repressed` is often taken as a broad worldview when `return of the repressed` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `return of the repressed`.
- Consequence: readers then import claims into `return of the repressed` that the corpus does not actually support for `return of the repressed`.
- Skill response: keep `return of the repressed` attached to the texts, cases, and institutional situations that make `return of the repressed` legible.
- Modern correction: show what `return of the repressed` can illuminate without pretending `return of the repressed` answers everything nearby.
- Language fix: name the overreach around `return of the repressed` directly instead of just gesturing at nuance.
- Reader test: if `return of the repressed` suddenly explains every problem, `return of the repressed` is being misread.

### return of the repressed — Boundary Condition

- Boundary condition: `return of the repressed` only travels well when the receiving problem shares enough structure with the original one behind `return of the repressed`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `return of the repressed`.
- Ethical test: some uses of `return of the repressed` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `return of the repressed` into careless advice.
- Good limit: it is fine for `return of the repressed` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `return of the repressed` has no boundary because `return of the repressed` feels generically wise.
- Operational question: under what conditions should `return of the repressed` be left unused?

### return of the repressed — Better Question

- Better question: instead of asking whether `return of the repressed` is simply true, ask what problem `return of the repressed` was trying to solve.
- Reader upgrade: this turns `return of the repressed` from a label into a live diagnostic instrument.
- Research upgrade: `return of the repressed` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `return of the repressed` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `return of the repressed` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `return of the repressed` becomes useful when `return of the repressed` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `return of the repressed` is framed as a problem-solving move?

### return of the repressed — Example Move

- Example move: use `return of the repressed` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `return of the repressed` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `return of the repressed` is strongest when `return of the repressed` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `return of the repressed` lets the user do that they could not do before.
- Transfer discipline: keep the move around `return of the repressed` small and explicit so the analogy does not sprawl.
- Repository standard: if `return of the repressed` never produces a concrete move, `return of the repressed` is not yet operational.
- User question: what is one thing `return of the repressed` would make me inspect differently right now?

### return of the repressed — What To Preserve

- Preserve the pressure: `return of the repressed` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `return of the repressed` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `return of the repressed`.
- Preserve the routing value: `return of the repressed` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `return of the repressed` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `return of the repressed` stops is part of preserving `return of the repressed` well.
- Preserve the usefulness: a transferred `return of the repressed` should still help someone make a cleaner judgment.

### return of the repressed — What To Drop

- Drop the costume: `return of the repressed` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `return of the repressed` has been understood.
- Drop the universal claim: `return of the repressed` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `return of the repressed` survives only as a catchphrase, `return of the repressed` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `return of the repressed` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `return of the repressed` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `return of the repressed` and remove the surrounding noise.

### return of the repressed — Design Implication

- Design implication: `return of the repressed` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `return of the repressed` that later edits can stay honest.
- UX implication: users should feel `return of the repressed` changing the angle of analysis, not just the tone.
- Evaluation implication: test `return of the repressed` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `return of the repressed` only if they sharpen the boundary of `return of the repressed` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `return of the repressed` should refuse.
- Bottom line: `return of the repressed` belongs in the repo only if `return of the repressed` keeps improving judgment after the historical costume is removed.

## 原始素材文件

这份 research 直接回链到本仓库的原始素材层：

- books：[interpretation-of-dreams.md](../sources/books/interpretation-of-dreams.md)、[introductory-lectures.md](../sources/books/introductory-lectures.md)、[psychopathology-of-everyday-life.md](../sources/books/psychopathology-of-everyday-life.md)
- transcripts：（无）
- articles：[britannica-freud.md](../sources/articles/britannica-freud.md)、[sep-freud.md](../sources/articles/sep-freud.md)
